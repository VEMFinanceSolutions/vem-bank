server:
  port: 9091

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/vem-account-db
    username: postgres
    password: vem123456

  jpa:
    hibernate:
      ddl-auto: create